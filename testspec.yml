version: 0.2
phases:
  install:
    commands:
      - echo install started
      - apt-get update -y
      - pip install -r requirements.txt
  build:
    commands:
      - echo test started
      - python root/manage.py  test --settings msa_app.ci_settings